- Installation of Git
- Architecture of Git
- Git Initialization
- Configuration of Git
  |- git config --global username  
  |- git config --global email
  |- git config --list
- Linux Few Commands
  |- touch
  |- ls (list)
  |- cd (Change directory)
  |- rm (remove files/folders)
  |- mv (rename or move the files/folders)
  |- cp (copy files/folders)
  |- cat (contatination)
- Git Commands
  |- git status
  |- git init
  |- git add
  |- git commit
  |- git diff
  |- git show
  |- git commit -a -m "commits"
- move, rename, delete file using git
  |- git rm <filename>
  |- git mv <file1> <file2>
- How to check git commits
  |- git show <commitname>
  |- git log
  |- git log --oneline
  |- git log --pretty = online
  |- git log --pretty = short
  |- git log --pretty = long
|- What is the difference between author and commit
|- how to unmodify, unstage, delete the commit
  |- git checkout --filename
  |- git checkoout -f
  |- git restore --stage <filename>
  |- git reset --hard <commitId>
|- git alias
  |- git config --global alias.st status
  |- git config --global alias.cm commit
******************************************************************
|- Working with remote directory
  |- create github account
  |- create ssh key and add to the remote github account
    |- ssh-keygen -t ed25519 -C "nilesh.1234avhad@gmail.com"
	|- eval "$(ssh-agent -s)"
	|- ssh-add ~/.ssh/id_ed25519
	|- ls -al ~/.ssh
	|- cat ~/.ssh/id_ed25519.pub
  |- add ssh key to remote github account
  |- create remote connection with remote repository
    |- git remote add origin <URL of remote directory>
	|- git clone
	|- git pull
	|- git push
|- git branching statergy
  |- architechure
  |- creation of new branch from old branch
    |- git checkout -b <new-branch-name>
	|- git checkout branch 
	|- git branch
  |- merging he branch
    |- git merge <branch-name>
  |- conflict resolution
*********************************
Interview Git Question.
1. Let me know how do you configure the git at your local system?
2. how to commit without staging.
3. How does git understand that rename or move with git mv command?
4. What does git clone do?
5. What does the command git config do?
6. What is a conflict and how do you resolve?
7. What does git status command do?
8. How will you create a git repository?
9. Tell me something about git stash?
10. What is the command used to delete a branch?
11. What differentiates between the commands git remote and git clone?
12. Differentiate between git pull and git fetch.
13. What command helps us know the list of branches merged to master?
14. How to revert a bad commit which is already pushed?
15. Can you tell the differences between git revert and git reset?
xbjascxhcjsgajxbcsj
